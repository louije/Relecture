<h1>Available services</h1>

<div class="row">
	<div class="available-services col-sm-6">
		<% @services.each do |s| %>
			<p class="service-button">
				<%= link_to "Log in to #{s.to_s.titleize}", log_in_service_path(s), class: "btn btn-default btn-block" %>
			</p>
		<% end %>
	</div>	
</div>